interface CartSummaryProps { subtotal: number; discount: number; total: number; disabled: boolean }
const format = (amount: number) => new Intl.NumberFormat('es-CO', { style: 'currency', currency: 'COP' }).format(amount)
export function CartSummary({ subtotal, discount, total, disabled }: CartSummaryProps) { return <aside className="cart-summary"><h2>Resumen</h2><dl><div><dt>Subtotal</dt><dd>{format(subtotal)}</dd></div><div><dt>Descuentos</dt><dd>{discount ? `-${format(discount)}` : '—'}</dd></div><div className="cart-total"><dt>Total</dt><dd>{format(total)}</dd></div></dl><div className="cart-promo"><input aria-label="Código promocional" placeholder="Código promocional" /><button type="button">Aplicar</button></div><p>Los impuestos se calcularán en el pago.</p><button className="cart-checkout" disabled={disabled} type="button">Realizar pedido</button></aside> }
